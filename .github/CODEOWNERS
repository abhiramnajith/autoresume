# @abhiramnajith owns everything. Combined with branch protection's "require
# review from Code Owners", this means every pull request needs approval from
# @abhiramnajith before it can merge to a protected branch (main).
#
# Why CODEOWNERS instead of a push restriction: per-user "restrict who can push"
# is an organization-only feature on GitHub. On a personal repo, requiring a
# code-owner review is the enforceable way to keep merges gated on the owner.
* @abhiramnajith
